# This .gitignore file uses an **allowlist** approach to specify which files
# and directories should be tracked by Git.

# Ignore everything in the root directory by default
*

# Allow specific files
!README.md
!LICENSE

# Allow specific directories (and specific files/patterns within them as needed)
!settings_utils/
!team_utils/

# Allow .py, .sh, and .md files in subdirectories
!**/*.py
!**/*.sh
!**/*.md
